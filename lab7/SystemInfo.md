### Processor info

command `lscpu`
output in screenshot `output1`

### RAM info

command `free -h`
output in sreeanshot `output2`

### Network info

installed by `sudo apt install net-tools` to use command `ifconfig`
output in screenshot `output3`

### OS

command `uname -a`
output in screenshot `output4`
